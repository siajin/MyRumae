.venv/
__pycache__/
*.pyc

# secrets — .env.example 은 커밋 OK, 실제 .env 는 금지
.env
!.env.example
backend/.env

# Per-user runtime state (DB, parsed cache, temp staging, downloads).
# master/ is committed — ships with the app via store update.
backend/data/user/
backend/playwright-state/

playwright-state/

*.db
*.sqlite3

node_modules/
dist/

# Tauri (Rust build artifacts and generated capability schema)
src-tauri/target/
src-tauri/gen/
src-tauri/WixTools/

# Frontend build output
frontend/dist/

uclass_references/